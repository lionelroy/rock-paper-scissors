
let 

function computerPlay() {
    // random return (rock || paper || scissors)
}

function playRound(playerSelection, computerSelection) {
    if(playerSelection = winner)
    return ("You win!");
    else if(computerSelection = winner)
    return ("you Lose!")
    else
    return ("It's a draw!")
}

function game() {
    // Loop 10 times
    function playRound() {
        // Keep score, reports loser or winner
    }
}